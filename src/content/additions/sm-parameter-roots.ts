import type { Lesson } from '../types.ts';

// 既存の quadratic-inequality は x の範囲、math2-algebra は解法・解と係数が中心。
// 本記事では「根を指定区間に置くためのパラメータ条件」と定義域内の解の数を扱う。
export const lesson: Lesson = {
  id: 'sm-parameter-roots',
  title: 'パラメータ付き二次方程式——根の配置と定義域',
  summary: '判別式だけでは決まらない「根がどこにあるか」を、放物線の軸と端点の符号で調べる。区間内に異なる2根を置く条件を導き、端点・重解・定義域を含む場合分けへ進む。',
  objectives: [
    '実数解の存在条件と、指定区間に解が入る条件を区別できる',
    '判別式・軸・端点の値から、異なる2根が開区間にある必要十分条件を導ける',
    '区間の端を含むかどうかと重解の数え方を区別して境界値を確認できる',
    '定義域に制限がある方程式で、範囲外の根を除いて解の個数を数えられる',
  ],
  blocks: [
    { type: 'heading', level: 3, content: '1. 解があることと、欲しい場所にあること' },
    { type: 'text', content: '時刻を計算して答えが2つ出ても、片方が開始前の負の時刻なら、開始後の答えとしては使えません。方程式が作る候補と、問題が許す範囲は別です。ここではこの「候補を区間で選ぶ」考え方を、係数に文字を含む二次方程式で練習します。以下の計算では文字はすべて実数、数値は単位をもたないものとします。' },
    { type: 'text', content: 'パラメータとは、式の形を調整するつまみのような文字です。たとえば x² − 2mx + m + 2 = 0 では、m を一つ固定してから x を探します。「異なる2根が 0 < x < 4 にあるような m」を求めるとき、答えるのは x の値ではなく、条件を満たす m の範囲です。根とは方程式を満たす x の値のことです。' },
    { type: 'note', variant: 'warn', content: '判別式が正なら異なる実数解が2つありますが、その場所は未確定です。たとえば (x − 5)(x − 6) = 0 は異なる2実数解をもちますが、区間 0 < x < 4 の解はありません。「実数全体」と「指定された定義域」を分けましょう。' },
    { type: 'heading', level: 3, content: '2. 放物線の谷底と、両岸を見る' },
    { type: 'text', content: '谷を通る道と水平な水面を想像してください。谷底が水面より下なら、道は水面と左右で交わります。ただし、指定した2本の柵の間に交点が両方あるには、谷底が柵の間にあり、左右の柵の位置では道が水面より上に戻っていることも必要です。これを放物線に翻訳します。' },
    { type: 'formula', tex: String.raw`f(x)=ax^2+bx+c=a(x-h)^2-\frac{D}{4a},\qquad h=-\frac{b}{2a},\quad D=b^2-4ac\quad(a>0)`, display: true },
    { type: 'text', content: 'ここでは上に開く放物線、つまり a > 0 とします。h は軸の位置、谷底の高さは −D/(4a) です。D > 0 は谷底が x 軸より下にあることを表します。a < 0 なら方程式全体に −1 を掛けてから同じ方法を使えます。a がパラメータを含む場合、a = 0 は二次方程式でなくなるので、最初に分けます。' },
    { type: 'derivation', title: '異なる2根を L < x < U に入れる条件（L < U、a > 0）', steps: [
      { label: 'まず2つの交点を作る', tex: String.raw`D>0`, note: '異なる2実根を α < β とおく。D = 0 は1つの値しかもたない重解なので除く。' },
      { label: '交点の中点を区間内へ', tex: String.raw`h=\frac{\alpha+\beta}{2},\qquad L<h<U`, note: '両根が区間内にあれば、その中点である軸も区間内にある。' },
      { label: '区間の端を根より外に出す', tex: String.raw`f(L)>0,\qquad f(U)>0`, note: '上に開く放物線は根の外側で正、根の間で負になる。' },
      { label: '4条件の共通部分を取る', tex: String.raw`L<\alpha<\beta<U\ \Longleftrightarrow\ D>0,\ L<h<U,\ f(L)>0,\ f(U)>0`, note: '逆向きも確かめよう。L < h かつ f(L) > 0 なら L は左根より左。U > h かつ f(U) > 0 なら U は右根より右。よって両根が区間内に収まる。' },
    ] },
    { type: 'note', variant: 'warn', content: '端点の値が両方正、だけでは不十分です。f(x) = (x − 5)(x − 6) は f(0) = 30、f(4) = 2 と両方正でも、根は両方とも右側にあります。軸 h = 5.5 が区間外だと見抜けば、この配置を除けます。一方、軸と端点だけを見ても f(x) = (x − 2)² + 1 のように実根がない場合があるので、判別式も必要です。' },
    { type: 'example', title: '例題1：異なる2根がともに 0 < x < 4 にある', body: '方程式 x² − 2mx + m + 2 = 0 の異なる2実根がともに 0 < x < 4 にあるための、m の範囲を求めましょう。まず「判別式、軸、左端、右端」の4行を自分で書いてください。', answer: String.raw`f(x) = x² − 2mx + m + 2 とおきます。D = 4(m² − m − 2) = 4(m − 2)(m + 1) > 0 より m < −1 または m > 2。軸 h = m より 0 < m < 4。左端 f(0) = m + 2 > 0 より m > −2。右端 f(4) = 18 − 7m > 0 より m < 18/7。すべての共通部分は $2<m<\frac{18}{7}$ です。m = 2 では (x − 2)² = 0 となり、異なる2根ではありません。m = 18/7 では根が 8/7 と 4 となり、4 が区間から外れます。内側の m = 5/2 なら根は $(5\pm\sqrt{7})/2$（約 1.177、3.823）で、確かに両方区間内です。近似値は位置確認用で、条件の導出には使っていません。` },
    { type: 'heading', level: 3, content: '3. 一点を挟む場合は、符号が近道になる' },
    { type: 'text', content: '2根があるとき、上に開く放物線はその間だけ x 軸より下にあります。そこで、ある数 r を2根が左右から挟むか知りたいだけなら、r での高さが負かを見れば足ります。谷底以下の高さにはならないので、f(r) < 0 なら谷底も負、つまり D > 0 まで自動的に保証されます。' },
    { type: 'formula', tex: String.raw`a>0:\qquad \alpha<r<\beta\ \Longleftrightarrow\ f(r)<0`, display: true },
    { type: 'example', title: '例題2：1より小さい根と、1より大きい根をもつ', body: '例題1と同じ x² − 2mx + m + 2 = 0 が、1 より小さい根と 1 より大きい根をもつための m の範囲を求めましょう。今回は 0 < x < 4 という制限はありません。', answer: '1 が2根の間にあればよいので、f(1) = 1 − 2m + m + 2 = 3 − m < 0。答えは m > 3 です。m = 3 では根は 1 と 5 であり、「1より小さい」を満たしません。m = 4 では根は 4 ± √10（約 0.838、7.162）で、1 を左右から挟みます。例題1とは要求する配置が違うため、条件も違います。' },
    { type: 'note', variant: 'warn', content: '「一点を挟む」と「区間内にちょうど1根」は別の問いです。f(L)f(U) < 0 なら開区間 (L, U) にちょうど1根ありますが、これは必要条件とは限りません。たとえば (x − 1)(x − 3) = 0 は (0, 3) 内に根 1 だけをもち、f(3) = 0 です。また (x − 1)² = 0 は (0, 2) 内に1つの解をもちますが、両端の積は正です。端点の根と重解を別に数える必要があります。' },
    { type: 'heading', level: 3, content: '4. 閉区間と定義域——境界を捨てない' },
    { type: 'text', content: '開区間 (L, U) は端を含まず、閉区間 [L, U] は端を含みます。異なる2根を閉区間に入れるなら、端点の正の条件を「0以上」に緩めます。ただし異なる2根という要求は変わらないので D > 0 はそのままです。軸も、異なる2根の中点なので引き続き L < h < U です。' },
    { type: 'formula', tex: String.raw`a>0,\ L<U:\quad L\le\alpha<\beta\le U\ \Longleftrightarrow\ D>0,\ L<h<U,\ f(L)\ge0,\ f(U)\ge0`, display: true },
    { type: 'text', content: '定義域が x ≥ 0 のときは、負の根は解として採用しません。異なる解の個数を数えるなら、重解は同じ数が繰り返されているだけなので1個です。まず実数全体の候補を求め、次に定義域でふるい分けると、判別式と定義域の役割を混同しません。' },
    { type: 'example', title: '例題3：x ≥ 0 の範囲だけで解を数える', body: 'x² − 2mx + m = 0 について、定義域を x ≥ 0 としたときの異なる解の個数を m によって分類しましょう。', answer: 'D = 4m(m − 1) なので、m < 0 または m > 1 では異なる2実根、m = 0, 1 では重解、0 < m < 1 では実根なしです。2根の積は m、和は 2m。m < 0 では積が負なので正負1根ずつあり、定義域内は1個。m > 1 では積が正で同符号、和が正なので両方正となり2個です。m = 0 の重解は 0、m = 1 の重解は 1 で、どちらも定義域内なので1個。0 < m < 1 は0個です。' },
    { type: 'table', headers: ['m の範囲・値', '実数全体の根', 'x ≥ 0 の解の個数'], rows: [
      ['m < 0', '負と正の2根', '1個'],
      ['m = 0', '重解 0', '1個'],
      ['0 < m < 1', '実根なし', '0個'],
      ['m = 1', '重解 1', '1個'],
      ['m > 1', '正の2根', '2個'],
    ] },
    { type: 'note', variant: 'tip', content: '例題3で定義域を x > 0 に変えると、m = 0 の行だけが0個に変わります。「以上」と「より大きい」の1文字の違いは、端点の解を数えるかどうかの違いです。' },
    { type: 'heading', level: 3, content: '5. 条件を立てた後の点検手順' },
    { type: 'list', ordered: true, items: [
      '最高次の係数が0になるパラメータを先に分け、二次式として扱える範囲を確認する。',
      '「異なる2根」「重解も可」「定義域内でちょうど1個」のどれかを読み、数直線に端点と根の目標位置を書く。',
      '2根を区間内に入れるなら判別式・軸・両端、一点を挟むならその点の符号、というように配置に合う条件を立てる。',
      '得たパラメータ条件はすべて同時に満たす共通部分を取り、境界値を元の方程式に戻して確かめる。',
    ] },
    { type: 'practice', title: '練習：配置を言葉から式に直す', problems: [
      { body: 'x² − 2mx + m + 2 = 0 が異なる2実根をもち、両方とも 1 より大きくなる m の範囲を求めよ。', hint: '上に開く場合、D > 0、軸が1より右、f(1) > 0 を組み合わせる。', answer: 'D > 0 より m < −1 または m > 2。軸 h = m > 1、f(1) = 3 − m > 0 より m < 3。共通部分は 2 < m < 3。軸が1より右で f(1) > 0 なら、1 は小さい方の根より左にある。m = 2 は重解、m = 3 は根1を含むので除く。' },
      { body: '例題1で区間を 0 ≤ x ≤ 4 に変更したとき、異なる2根がともにこの区間にある m の範囲を求めよ。', answer: '2 < m ≤ 18/7。D > 0 と 0 < h < 4 は維持し、f(0) ≥ 0、f(4) ≥ 0 とする。m = 18/7 の根 8/7 と4は両方採用できるが、m = 2 は重解なので依然として除く。' },
      { body: 'x² − 2x + k = 0 の異なる2実根がともに −1 < x < 3 にある k の範囲を求めよ。', answer: '軸は h = 1 で常に区間内。D = 4 − 4k > 0 より k < 1、両端の値はともに 3 + k > 0 より k > −3。答えは −3 < k < 1。k = −3 では根 −1, 3 が両端に来て、k = 1 では重解1になるので、いずれも除く。' },
      { body: 'mx² − 2x + 1 = 0 が異なる正の実数解を2つもつ m の範囲を求めよ。m = 0 も確認せよ。', answer: 'm = 0 では一次方程式 −2x + 1 = 0 となり解は 1/2 の1個。m ≠ 0 では D = 4 − 4m > 0 より m < 1。2根の積 1/m が正であるためには m > 0 が必要で、このとき和 2/m も正なので両根は正。答えは 0 < m < 1。m = 1 は重解1であり異なる2解ではない。' },
    ] },
    { type: 'quiz', title: '確認：必要な条件を取り違えない', questions: [
      { question: 'a > 0、L < U のとき、異なる2根がともに (L, U) にあるための必要十分条件は？（D は判別式、h は軸）', choices: ['D > 0 のみ', 'f(L) > 0 かつ f(U) > 0 のみ', 'D > 0、L < h < U、f(L) > 0、f(U) > 0 のすべて', 'D ≥ 0 かつ L < h < U のみ'], answerIndex: 2, explanation: '判別式が交点の存在、軸が谷の位置、両端の符号が根より外にあることを保証する。4条件の共通部分が必要。' },
      { question: 'x² − 2mx + m + 2 = 0 の2根が 1 を厳密に左右から挟む条件は？', choices: ['m < 3', 'm = 3', 'm ≥ 3', 'm > 3'], answerIndex: 3, explanation: 'f(1) = 3 − m が負なら1は2根の間にある。等号では1自身が根になるため除く。' },
      { question: 'x² − 2mx + m = 0 に m = −1 を代入し、x ≥ 0 の異なる解を数えると？', choices: ['0個', '1個', '2個', '無数にある'], answerIndex: 1, explanation: 'x² + 2x − 1 = 0 の根は −1 ± √2。片方だけが正なので定義域内は1個。D > 0 でも定義域内に2個とは限らない。' },
      { question: '異なる2根を開区間 (L, U) から閉区間 [L, U] に入れる問題へ変えた。どの扱いが正しい？（a > 0、L < U）', choices: ['D > 0 は維持し、端点の値を0以上とする', 'D = 0 だけを調べる', '端点の値は調べなくてよい', '重解を異なる2解として数える'], answerIndex: 0, explanation: '閉区間では端点の根を認めるので f(L), f(U) は0でもよい。異なる2根の指定は変わらないため、重解を認める D ≥ 0 には変更しない。' },
    ] },
  ],
};
