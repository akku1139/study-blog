import type { Lesson } from '../types.ts';

export const lesson: Lesson = {
  id: 'um-least-squares',
  title: '最小二乗法と射影 — 残差から直線当てはめとQRへ',
  summary: '観測値を列空間へ近づけるという見方から、正規方程式の必要十分性、中心化した直線当てはめ、係数の一意性、QRによる解法を一つのデータでつなぎます。',
  objectives: [
    '最小二乗法が最小化する距離と、散布図上の垂直方向の残差を区別して説明できる',
    '残差の直交性から正規方程式を導き、それが最小値を与えることを示せる',
    '中心化したデータから傾きと切片を求め、残差で検算できる',
    '予測値の一意性と係数の一意性を区別し、QRで同じ射影を計算できる',
  ],
  blocks: [
    { type: 'heading', level: 3, content: '1．全部を通れないなら、何を近づけるか' },
    { type: 'text', content: '測定値にばらつきがあると、一本の直線で全点を通ることはできません。そこで「上に外れた量と下に外れた量を相殺する」のではなく、各ずれを二乗して足します。大きなずれほど強く罰する、という選択です。ここでは説明変数 t を固定し、応答 y を y≈a+ct で表す通常の最小二乗法を考えます。例題のデータは説明用の自作・無次元データで、分数は近似でなく厳密値です。' },
    { type: 'formula', tex: String.raw`S(a,c)=\sum_{i=1}^{m}(y_i-a-ct_i)^2,\qquad \widehat y_i=a+ct_i,\quad r_i=y_i-\widehat y_i`, display: true },
    { type: 'note', variant: 'warn', content: 'この残差は散布図での y 方向のずれです。散布図上で各点から直線へ下ろした垂線の長さを最小にする方法とは違います。後でいう「直交」は、m 個の応答を並べたベクトル空間での直交です。物理量なら r は y と同じ単位、S はその単位の二乗、c は y の単位を t の単位で割ったものです。' },
    { type: 'heading', level: 3, content: '2．調節できる予測値は列空間に並ぶ' },
    { type: 'text', content: '切片を変えると全観測の予測値が同じだけ動き、傾きを変えると各予測値が t に比例して動きます。この二つの動かせる方向を行列の列に並べます。一般には実 m×n 行列 A、係数ベクトル x、観測ベクトル b を用い、予測 Ax を作ります。直線の場合だけ x=(a,c)ᵀ、b=(y₁,…,yₘ)ᵀ です。' },
    { type: 'formula', tex: String.raw`A=\begin{pmatrix}1&t_1\\\vdots&\vdots\\1&t_m\end{pmatrix},\qquad x=\begin{pmatrix}a\\c\end{pmatrix},\qquad \min_{x\in\mathbb R^n}\|b-Ax\|_2^2`, display: true },
    { type: 'text', content: 'A の列の線形結合全体を列空間 Col(A) と呼びます。予測 Ax はこの空間から出られません。床の上から動けない点を、床の上方にある点へ近づけるなら、真下まで動かすのが最短です。同じ考えで b に最も近い列空間の点を探す操作が正射影です。有限次元の部分空間なので、この最も近い予測値は必ず存在します。' },
    { type: 'heading', level: 3, content: '3．なぜ残差は動かせる方向に直交するのか' },
    { type: 'text', content: '最適候補を x̂、残差を r=b−Ax̂ とします。もし残差に「まだ動かせる方向」の成分が残っていたら、その方向へ少し動くことで距離を短くできるはずです。この直感を、任意の係数方向 h に沿う一変数の二次式で確かめます。' },
    { type: 'derivation', title: '方向を一本ずつ調べて正規方程式を得る', steps: [
      { label: '係数を実数 s だけ動かす', tex: String.raw`f(s)=\|b-A(\widehat x+sh)\|_2^2=\|r\|_2^2-2s\,h^TA^Tr+s^2\|Ah\|_2^2`, note: '転置 T と標準内積を用いた二乗の展開です。h は任意です。' },
      { label: '最小なら s=0 で傾きは0', tex: String.raw`f'(0)=-2h^TA^Tr=0\ \text{for all }h\quad\Longrightarrow\quad A^Tr=0`, note: 'すべての座標方向 h について0なので、Aᵀr の各成分が0です。' },
      { label: '残差の定義を戻す', tex: String.raw`A^T(b-A\widehat x)=0\quad\Longleftrightarrow\quad A^TA\widehat x=A^Tb`, note: 'これを正規方程式と呼びます。正規分布を仮定した式ではありません。' },
    ] },
    { type: 'text', content: '停留点を求めただけでは一般に最小とは限りません。しかしこの問題では、直交性があれば下のピタゴラスの等式が成り立つため、大域的な最小値だと保証できます。つまり正規方程式は、実数・制約なし・通常の二乗ノルムの最小二乗問題における必要十分条件です。' },
    { type: 'formula', tex: String.raw`A^Tr=0\quad\Longrightarrow\quad \|b-A(\widehat x+h)\|_2^2=\|r\|_2^2+\|Ah\|_2^2\ge\|r\|_2^2`, display: true },
    { type: 'example', title: '例題1：一方向への射影を目で追う', body: String.raw`$A=(1,1,0)^T$、$b=(1,3,2)^T$ とする。係数 x は実数一つである。最小二乗解、予測、残差を求めよ。`, answer: String.raw`$A^TA=2$、$A^Tb=4$ より $\widehat x=2$。予測は $(2,2,0)^T$、残差は $(-1,1,2)^T$、二乗和は6です。残差と A の内積は −1+1=0。直接平方完成しても $(1-x)^2+(3-x)^2+4=2(x-2)^2+6$ となり一致します。第3成分はどの x でも0としか予測できず、残差2は消せません。` },
    { type: 'heading', level: 3, content: '4．一意なのは係数か、予測か' },
    { type: 'text', content: '上の等式で別の係数 x̂+h も最小になるのは Ah=0 のときだけです。そのとき予測 A(x̂+h) は Ax̂ と同じです。したがって予測は常に一意ですが、係数まで一意になるには A の列が一次独立でなければなりません。同じ働きをする列があれば、係数の分担を変えても予測が変わらないからです。' },
    { type: 'formula', tex: String.raw`h^TA^TAh=\|Ah\|_2^2,\qquad \operatorname{rank}(A)=n\ \Longleftrightarrow\ A^TA\text{ は正定値}\quad\Longrightarrow\quad \widehat x=(A^TA)^{-1}A^Tb`, display: true },
    { type: 'note', variant: 'warn', content: '観測数 m が係数数 n 以上でも、列が独立とは限りません。正規方程式自体は列従属でも成立し、解も存在しますが、逆行列の公式は使えません。逆行列は理論式として便利でも、計算では連立方程式を直接解くかQRなどを使います。' },
    { type: 'heading', level: 3, content: '5．中心化すると直線の係数の意味が見える' },
    { type: 'text', content: '切片を含む直線では列が 1 と t なので、直交性は「残差の和が0」「t を掛けた残差の和が0」です。まず一つ目を使うと、当てはめ直線がデータの平均点を通ることがわかります。そこを原点に移す中心化を行えば、切片と傾きの計算が分離します。' },
    { type: 'derivation', title: '平均点を通る直線の傾きを求める', steps: [
      { label: '定数列への直交性', tex: String.raw`\sum_i(y_i-a-ct_i)=0\quad\Longrightarrow\quad a=\bar y-c\bar t`, note: 'm≥1、平均はそれぞれの和を m で割ったものです。' },
      { label: 't の列への直交性に代入', tex: String.raw`\sum_i(t_i-\bar t)\{(y_i-\bar y)-c(t_i-\bar t)\}=0`, note: '中心化した t の和が0なので、平均の成分が消えます。' },
      { label: '少なくとも二つの異なる t があるとき', tex: String.raw`S_{tt}=\sum_i(t_i-\bar t)^2>0,\qquad c=\frac{\sum_i(t_i-\bar t)(y_i-\bar y)}{S_{tt}},\qquad a=\bar y-c\bar t`, note: '分母が0、つまり全 t が同じときにはこの割り算はできません。' },
    ] },
    { type: 'example', title: '例題2：4点を当てはめ、残差で検算する', body: '自作データ (t,y)=(−1,0),(0,1),(1,1),(2,4) に切片ありの最小二乗直線を当てはめる。平均を引いてから、傾きと切片、残差の二乗和を求めよ。', answer: '平均は t̄=1/2、ȳ=3/2。中心化した t は (−3/2,−1/2,1/2,3/2)、y は (−3/2,−1/2,−1/2,5/2)。t の二乗和は5、積の和は6なので c=6/5、a=9/10。予測は (−3/10,9/10,21/10,33/10)、残差は (3/10,1/10,−11/10,7/10)。残差の和も t を掛けた和も0です。二乗和は (9+1+121+49)/100=9/5。残差が正負に分かれること自体ではなく、二つの内積が0であることが最小の証拠になります。' },
    { type: 'example', title: '例題3：全て同じ t なら何がわからないか', body: 'データ (t,y)=(2,1),(2,2),(2,6) を y≈a+ct で当てはめる。係数が一意になるかを判断せよ。', answer: '予測は全て a+2c=s です。二乗和は (1−s)²+(2−s)²+(6−s)²=3(s−3)²+14。したがって a+2c=3 を満たすすべての係数が解です。例えば (a,c)=(3,0) と (1,1) は同じ予測 (3,3,3) を与え、最小二乗和は14。観測が3点あっても、t の変化がないので傾きは識別できません。' },
    { type: 'heading', level: 3, content: '6．QRは同じ射影を別の座標で計算する' },
    { type: 'text', content: '列が斜めに交わっていると、ある係数の変更が他の方向にも影響します。列空間の基底を互いに直交する単位ベクトルへ取り替えると、その方向の成分を内積だけで読み取れます。m≥n かつ列が一次独立の A の薄いQR分解 A=QR では、Q は m×n、R は可逆な n×n 上三角行列です。' },
    { type: 'formula', tex: String.raw`Q^TQ=I_n,\qquad b=QQ^Tb+(I_m-QQ^T)b,\qquad \|b-QRx\|_2^2=\|Q^Tb-Rx\|_2^2+\|(I_m-QQ^T)b\|_2^2`, display: true },
    { type: 'text', content: '右辺第2項は列空間に垂直な、動かせない成分です。第1項を0にすれば最小なので Rx̂=Qᵀb を後退代入で解きます。予測は QQᵀb、残差は (I−QQᵀ)b。P=QQᵀ は Pᵀ=P、P²=P を満たす射影行列です。列独立なら P=A(AᵀA)⁻¹Aᵀ とも書け、正規方程式とQRは厳密演算では同じ予測を与えます。' },
    { type: 'note', variant: 'warn', content: '薄い Q は列が正規直交するだけで、m>n のとき QQᵀ=Iₘ ではありません。もしそうなら全ての b の残差が0になってしまいます。また、AᵀA を作ると2ノルム条件数は κ₂(AᵀA)=κ₂(A)² となり、列がほぼ従属な場合に丸め誤差の影響が増えやすくなります。Householder変換によるQRはこの二乗化を避けますが、元の問題の悪条件性を消すわけではありません。階数落ちには列ピボット付きQRや特異値分解など、階数を意識した扱いが必要です。' },
    { type: 'example', title: '例題4：例題2をQRで解き直す', body: String.raw`例題2の $A$ の列は $(1,1,1,1)^T$ と $(-1,0,1,2)^T$。第1列を正規化し、第2列からその方向の成分を引いて、薄いQR分解を作れ。`, answer: String.raw`$q_1=(1,1,1,1)^T/2$。第2列と $q_1$ の内積は1なので、引き算すると $(-3/2,-1/2,1/2,3/2)^T$。長さは $\sqrt5$ で、これを割ったものが $q_2$ です。従って $R=\begin{pmatrix}2&1\\0&\sqrt5\end{pmatrix}$、$Q^Tb=(3,6/\sqrt5)^T$。下の式 $\sqrt5 c=6/\sqrt5$ から $c=6/5$、上の式 $2a+c=3$ から $a=9/10$。第2列を直交化する操作は t の中心化そのものでした。` },
    { type: 'heading', level: 3, content: '7．計算の保証とモデルの妥当性は別' },
    { type: 'text', content: 'ここまでの最小化に誤差の正規分布は不要です。一方、回帰係数の不偏性や信頼区間などには、誤差の平均・分散・独立性など別の仮定が必要です。残差が直交していても直線モデルが現象に適切とは限らず、外れ値に二乗が強く反応する点も残ります。通常の最小二乗は各観測を同じ重みで扱います。測定精度が異なる場合に正の対角成分をもつ対角行列 W を使う重み付き最小二乗では、直交条件も AᵀWr=0 に変わります。' },
    { type: 'list', ordered: true, items: [
      '予測式から A の列を作る。切片ありなら定数1の列を忘れない。',
      '列の独立性を確認し、正規方程式またはQRで解く。m≥n だけで一意と決めない。',
      '予測と残差を元のデータで計算し直し、Aᵀr=0 と二乗和を確認する。丸めを含む計算ではデータの尺度に応じた許容誤差で検査する。',
      '残差の形や外れ値も調べる。最小二乗解が求まったことと、外挿や因果解釈が妥当なことを混同しない。',
    ] },
    { type: 'practice', title: '練習：解を出したら直交性で確かめる', problems: [
      { body: String.raw`$A=(1,2)^T$、$b=(3,1)^T$ の最小二乗解、残差、最小二乗和を求めよ。`, hint: '係数は実数一つです。AᵀA と Aᵀb を計算します。', answer: String.raw`$A^TA=5$、$A^Tb=5$ より $\widehat x=1$。予測は $(1,2)^T$、残差は $(2,-1)^T$、二乗和は5です。内積 $2+2(-1)=0$ により最小であることも確認できます。` },
      { body: 'データ (t,y)=(−1,2),(0,0),(1,4) の切片ありの最小二乗直線と残差を求めよ。', hint: 't の平均が0なので切片は y の平均です。', answer: 't̄=0、ȳ=2、Sₜₜ=2、中心化した積の和は2なので c=1、a=2。予測は (1,2,3)、残差は (1,−2,1)、二乗和は6。残差の和は0、t を掛けた和も −1+1=0 です。' },
      { body: 'データ (t,y)=(1,2),(2,1) に、切片を固定して0にした直線 y=ct を当てはめる。残差の和は0になるか。', answer: 'c=(1×2+2×1)/(1²+2²)=4/5。残差は (6/5,−3/5)、和は3/5で0ではありません。しかし t との内積は6/5−6/5=0。定数列を A に入れなかったので、残差の和が0という条件は要求されません。' },
      { body: String.raw`薄いQR分解で $R=\begin{pmatrix}2&-1\\0&3\end{pmatrix}$、$Q^Tb=(1,6)^T$ とわかっている。最小二乗係数 $x=(x_1,x_2)^T$ を求めよ。`, answer: String.raw`$Rx=Q^Tb$ を下から解きます。$3x_2=6$ より $x_2=2$、$2x_1-x_2=1$ より $x_1=3/2$。R の対角要素がともに0でないので解は一意です。なおこの情報だけでは列空間の外の成分は不明なので、残差の大きさは決まりません。` },
    ] },
    { type: 'quiz', title: '確認クイズ：どの空間の直交か', questions: [
      { question: '実行列 A の通常の最小二乗問題で、r=b−Ax̂ が Aᵀr=0 を満たすとき、常に正しいのはどれか。', choices: ['r は必ず零ベクトルである', 'x̂ は最小二乗解で、予測 Ax̂ は一意である', 'AᵀA は必ず可逆である'], answerIndex: 1, explanation: '直交性による二乗和の分解が最小性を保証し、他の最小解との差は A の核に入るので予測は同じです。残差0や列独立までは保証しません。' },
      { question: '切片ありの直線当てはめで、係数が一意に決まるための t の条件はどれか。', choices: ['少なくとも二つの異なる t の値がある', '観測数が3以上なら t の値は何でもよい', '全ての t が同じ正数である'], answerIndex: 0, explanation: '異なる t があれば定数列と t の列は独立で、中心化した二乗和も正です。観測数だけでは独立性は保証されません。' },
      { question: 'm>n、列独立な A=QR の薄いQR分解で、QQᵀ は何を表すか。', choices: ['常に m×m 単位行列', '残差を必ず倍にする行列', 'Col(A) への正射影行列'], answerIndex: 2, explanation: 'Q の列方向の成分を Qᵀ で取り出し Q で戻すので射影です。QᵀQ=Iₙ でも QQᵀ=Iₘ ではありません。' },
      { question: '通常の最小二乗解を正規方程式よりQRで求める主な数値的利点はどれか。', choices: ['元の A が悪条件でも係数誤差を常に0にできる', 'AᵀA を明示的に作ることによる条件数の二乗化を避けられる', '階数落ちでも通常の逆行列を必ず作れる'], answerIndex: 1, explanation: '安定なQR法は正規方程式で生じる条件数の二乗化を避けます。ただし元の問題の感度や階数落ちは別途考慮が必要です。' },
    ] },
    { type: 'note', variant: 'info', content: '数学的根拠：標準内積による二乗の展開、有限次元部分空間への正射影、核と列独立性、薄いQR分解を用いました。条件数の式は列独立な実行列の2ノルムで、AᵀA の固有値が A の特異値の二乗になることから従います。本記事では既存の内積・行列分解の概説を前提に、中心化、残差による検算、同一データのQR解法と適用条件を掘り下げています。' },
  ],
};
